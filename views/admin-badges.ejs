<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Badges</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Reemplaza con la ruta correcta -->
    <style>
        /* Estilo adicional para la tabla */
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        th, td {
            text-align: left;
            padding: 12px;
            border-bottom: 1px solid #ccc;
        }

        th {
            background-color: #cccccc;;
            color: black;
        }

        td img {
            width: 30px;
            height: 30px;
        }

        .actions button {
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .actions .edit {
            background-color: #007bff;
            color: white;
        }

        .actions .delete {
            background-color: #dc3545;
            color: white;
        }

        .actions button:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
<%- include('partials/username-display') %>
<h1>Manage Badges</h1>
<table>
    <thead>
    <tr>
        <th>Image</th>
        <th>Range</th>
        <th>Min Bitpoints</th>
        <th>Max Bitpoints</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <% badges.forEach(badge => { %>
        <tr>
            <td><img src="<%= badge.png %>" alt="<%= badge.rango %>"></td>
            <td><%= badge.rango %></td>
            <td><%= badge.bitpoints_min %></td>
            <td><%= badge.bitpoints_max === 0 ? 'N/A' : badge.bitpoints_max %></td>
            <td class="actions">
                <button class="edit" onclick="window.open('/admin/badges/edit/<%= badge._id %>', '_blank')">Edit</button>
                <form action="/admin/badges/delete/<%= badge._id %>" method="POST" style="display:inline;">
                    <button class="delete" type="submit">Delete</button>
                </form>
            </td>
        </tr>
    <% }); %>
    </tbody>
</table>
</body>
</html>
